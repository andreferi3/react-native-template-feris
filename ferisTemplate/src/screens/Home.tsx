import React from 'react'
import { View, Text } from 'react-native'

interface Props {
    
}

const Home = (props: Props) => {
    return (
        <View>
            <Text>Welcome To Feris React Native Template</Text>
        </View>
    )
}

export default Home
